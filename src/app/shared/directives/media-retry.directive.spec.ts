import { MediaRetryDirective } from './media-retry.directive';

describe('MediaRetryDirective', () => {
  it('should create an instance', () => {
    const directive = new MediaRetryDirective();
    expect(directive).toBeTruthy();
  });
});
